const plugin = require("tailwindcss/plugin")

module.exports = {
  theme: {
    extend: {
      boxShadow: {
        dropShadow: "10px 10px 0 0 #e4e6ea",
      },
      colors: {
      },
    },
  },
  plugins: [
  ],
}
